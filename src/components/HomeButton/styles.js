import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderRadius: 8,
  },
  button: {
    padding: 30,
  },
  text: {
    color: 'white',
    fontWeight: '600',
  },
});

export default styles;
